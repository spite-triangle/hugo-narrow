---
title: "Demo"
date: 2026-12-26
draft: false
description: "A complete guide to all available shortcodes for the Hugo Narrow theme"
---





我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：



我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：



我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：



我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：



我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：


我先看一下完整的 toc.html 文件，了解现有结构和 JS 交互逻辑。

我先看一下现有的 JS 交互逻辑，了解 toggle/dropdown 的开关机制。

现在我理解了完整的结构。需要做两件事：

HTML: 在 toc.html 的 "center" 模式分支中新增 section-center-toggle + section-center-dropdown
JS: 在 toc.js 中添加对应的交互逻辑
现在更新 toc.js 添加对应的交互逻辑：

检查 i18n 是否已有 ui.sections 键，以及图标是否有 folder：

需要为所有 13 种语言添加 ui.sections 翻译：